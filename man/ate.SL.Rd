% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random_ate.R
\name{ate.SL}
\alias{ate.SL}
\title{Estimate Average Treatment Effect (ATE) via SL}
\usage{
ate.SL(Y, A, mu1, mu0, pi1, pi0, min.pi = 0.05, max.pi = 0.95)
}
\arguments{
\item{Y}{Numeric vector. Observed outcomes for labelled data.}

\item{A}{Numeric vector. Treatment indicator (1 for treated, 0 for control).}

\item{mu1}{Numeric vector. Estimated \eqn{E[Y | A = 1, X]} for each individual.}

\item{mu0}{Numeric vector. Estimated \eqn{E[Y | A = 0, X]} for each individual.}

\item{pi1}{Numeric vector. Estimated propensity scores \eqn{P(A = 1 | X)}.}

\item{pi0}{Numeric vector. Estimated propensity scores \eqn{P(A = 0 | X)}.}

\item{min.pi}{Numeric. Lower bound to truncate estimated propensity scores (default = 0.05).}

\item{max.pi}{Numeric. Upper bound to truncate estimated propensity scores (default = 0.95).}
}
\value{
A list with:
\describe{
\item{est}{Estimated ATE.}
\item{se}{Standard error of the ATE estimator.}
}
}
\description{
Estimate Average Treatment Effect (ATE) via SL
}
\details{
This estimator uses the efficient influence function for ATE under fully supervised learning,
incorporating both outcome regression and inverse probability weighting.
}
